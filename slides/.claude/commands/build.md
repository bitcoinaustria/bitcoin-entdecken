---
description: Build the LaTeX presentation (format + build)
---

Format and build the LaTeX presentation following the project workflow:

1. Run `make format` to format the LaTeX source with latexindent
2. Run `make build` to compile the PDF with microtype enhancement

!cd 2025-missverstaendnisse && make format && make build

$ARGUMENTS